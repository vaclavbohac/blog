<%= content_tag @as, class: "text-base font-semibold tracking-tight text-zinc-800 dark:text-zinc-100", **@attrs do %>
  <% if @href %>
    <%= render CardComponent::LinkComponent.new(href: @href) do %><%= content %><% end %>
  <% else %>
    <%= content %>
  <% end %>
<% end %>
