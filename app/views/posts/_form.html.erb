<%= form_with(model: post) do |form| %>
  <% if post.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(post.errors.count, "error") %> prohibited this post from being saved:</h2>

      <ul>
        <% post.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :title, style: "display: block" %>
    <%= form.text_field :title %>
  </div>

  <div>
    <%= form.label :perex, style: "display: block" %>
    <%= form.textarea :perex %>
  </div>

  <div>
    <%= form.label :body, "Body (Markdown — used only when no template is set)", style: "display: block" %>
    <%= form.textarea :body %>
  </div>

  <div>
    <%= form.label :content_path, "Template (logical path, e.g. post_content/pattern_matching — leave blank for a Markdown article)", style: "display: block" %>
    <%= form.text_field :content_path %>
  </div>

  <div>
    <%= form.label :series_id, "Series", style: "display: block" %>
    <%= form.collection_select :series_id, Series.ordered, :id, :title, include_blank: "— none —" %>
  </div>

  <div>
    <%= form.label :position, "Position in series", style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.label :published_at, style: "display: block" %>
    <%= form.datetime_field :published_at %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
